<%= form_for([@github_account, @repo]) do |f| %>
  <% if @repo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@repo.errors.count, "error") %> prohibited this repo from being saved:</h2>

      <ul>
      <% @repo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :github_name %><br>
    <%= f.select :github_name, options_for_select(@possible_repos.map{|r| r[:full_name]}) %>
  </div>  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
